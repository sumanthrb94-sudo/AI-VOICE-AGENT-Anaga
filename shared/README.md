# shared

Shared contracts, schemas, and types across services.

| File | Contract |
|---|---|
| `call-api-contract.md` | the live-call brain — `/api/anaga/turn`, `/api/anaga/summary` |
| `integrations-contract.md` | the tubing — Meta Lead Ads webhook, lead intake, dial job, call outcome, CRM events |
| `persona.schema.json` | agent persona (voice, disclosure, languages) |
| `flow.schema.json` | versioned conversation flows |

See `engineering/MULTI_AGENT_SPEC.md` for the full work-package brief and
`docs/INTEGRATIONS.md` for the setup runbook.
